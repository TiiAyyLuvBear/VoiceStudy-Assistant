import {NavLink} from 'react-router-dom'; import {Activity,AudioLines,BookUser,Clock3,Mic,Settings,ShieldCheck,Users} from 'lucide-react'; import type {ReactNode} from 'react';
export function Badge({children,tone='neutral'}:{children:ReactNode;tone?:'neutral'|'success'|'warning'|'danger'|'info'}){return <span className={`badge ${tone}`}>{children}</span>}
export function Shell({children}:{children:ReactNode}){const links=[['Assistant','/',Mic],['Enroll Speaker','/enroll',AudioLines],['Speakers','/speakers',Users],['Preferences','/preferences',Settings],['Activity','/activity',Clock3]] as const; return <div className="app"><aside><div className="brand"><ShieldCheck/> <span>Secure Voice<br/><small>Assistant</small></span></div><nav>{links.map(([label,to,Icon])=><NavLink key={to} to={to} end={to==='/' }><Icon size={18}/>{label}</NavLink>)}</nav><div className="side-status"><Badge tone="success">Connected</Badge><small>VoiceStudy v1.0</small></div></aside><main><header><div><span className="eyebrow">SECURITY-FIRST VOICE CONTROL</span><h1>Secure Voice Assistant</h1></div><div className="header-status"><Badge tone="success">● Backend connected</Badge><span>Mic ready</span></div></header>{children}</main></div>}
export function Card({children,className='' }:{children:ReactNode;className?:string}){return <section className={`card ${className}`}>{children}</section>}
export function Status({label,detail,tone='info'}:{label:string;detail?:string;tone?:'info'|'success'|'warning'|'danger'}){return <div className={`status ${tone}`}><span className="status-dot"/><div><strong>{label}</strong>{detail&&<small>{detail}</small>}</div></div>}
