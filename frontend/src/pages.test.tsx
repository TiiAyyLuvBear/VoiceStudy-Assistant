import {render,screen} from '@testing-library/react';import userEvent from '@testing-library/user-event';import {MemoryRouter} from 'react-router-dom';import {describe,expect,it,vi} from 'vitest';import {AssistantPage,EnrollPage} from './pages';
vi.mock('./services/api',()=>({processAudio:vi.fn(async()=>({transcript:'Bây giờ là mấy giờ?',securityLevel:'public',response:'Backend response'})),enrollSpeaker:vi.fn()}));
describe('live API assistant flow',()=>{it('starts and stops microphone control',()=>{render(<MemoryRouter><AssistantPage/></MemoryRouter>);expect(screen.getByRole('button',{name:/start recording/i})).toBeInTheDocument()});});
describe('enrollment validation',()=>{it('requires five samples before upload',async()=>{const u=userEvent.setup();render(<MemoryRouter><EnrollPage/></MemoryRouter>);await u.type(screen.getByPlaceholderText('Nguyễn Văn B'),'Test User');await u.type(screen.getByPlaceholderText('user003'),'user003');await u.click(screen.getByRole('button',{name:/upload enrollment/i}));expect(await screen.findByText(/Exactly 5 WAV samples required/i)).toBeInTheDocument()})});
